import { FilesStorageInterceptor } from './files-storage.interceptor';

describe('FilesStorageInterceptor', () => {
  it('should be defined', () => {
    expect(new FilesStorageInterceptor()).toBeDefined();
  });
});
